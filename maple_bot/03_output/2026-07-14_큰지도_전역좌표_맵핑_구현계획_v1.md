# 큰 지도 전역좌표 맵핑 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 이동 창형 미니맵을 큰 지도 절대좌표에 맵핑하고, 사냥 구역 밖의 지정 이동점·액션점, 반복 루트, 일회성 목적지 이동을 기존 실행기에 통합한다.

**Architecture:** 큰 지도 픽셀을 영구 좌표로 사용하고 두 점 보정으로 로컬 미니맵 좌표를 변환한다. 별도 미니맵 창 추적기가 확정·추정 전역 위치를 공급하며, 전역 경로 실행기는 기존 `BlockRunner`와 `Humanizer`를 재사용한다. 기존 로컬 설정은 백업 후 한 번만 전역 좌표로 변환하고 전역 지도가 없으면 기존 실행 경로를 유지한다.

**Tech Stack:** Python 3.14, PyQt6, NumPy, OpenCV, pytest, 기존 ConfigManager·BlockRunner·Humanizer.

## Global Constraints

- 새 Python 파일 첫 줄에는 파일 역할을 설명하는 한 줄짜리 한국어 주석을 둔다.
- 큰 지도와 미니맵은 같은 방향과 가로세로 비율이며 회전·원근 변환은 지원하지 않는다.
- 구역 밖에서는 등록된 이동점·연결·액션점만 실행하고 자율 탐색은 하지 않는다.
- 액션 입력은 반드시 기존 `Humanizer`를 통과한다.
- 추적 실패 상태에서도 시간 제한 없이 전역 명령을 계속하며 상태를 `estimated`로 노출한다.
- 기존 `config.json`은 변환 전에 `config.pre_world_map_YYYYMMDD_HHMMSS.json`으로 백업한다.
- `world_map.enabled`가 거짓이거나 지도 로드에 실패하면 기존 로컬 모드를 그대로 사용한다.
- 각 작업은 해당 테스트를 먼저 실패시킨 뒤 최소 구현으로 통과시키고 한 문장으로 설명 가능한 커밋을 만든다.

---

## 파일 구조

- `core/navigation/world_map.py`는 전역 좌표, 보정, 노드·연결·루트·액션 데이터 모델과 검증만 담당한다.
- `core/navigation/viewport_tracker.py`는 미니맵 배경 매칭, 프레임 이동량, 확정·추정 창 원점만 담당한다.
- `core/navigation/world_graph.py`는 등록 연결망의 최단경로만 계산한다.
- `core/navigation/world_runner.py`는 전역 경로를 기존 `BlockRunner`로 실행하고 액션점을 `Humanizer`로 실행한다.
- `core/navigation/world_migration.py`는 기존 로컬 설정의 백업과 전역 좌표 변환만 담당한다.
- `core/sensing/world_position_scanner.py`는 미니맵 캡처와 전역 위치 공급자 갱신만 담당한다.
- `core_ui/world_map_canvas.py`는 큰 지도, 창, 영역, 노드, 연결의 표시와 좌표 선택만 담당한다.
- `core_ui/world_map_editor.py`는 이미지 불러오기, 두 점 보정, 편집 도구, 목적지·루트 실행 요청을 담당한다.
- 기존 `core/runtime.py`, `core/config_adapter.py`, `core/config_manager.py`, `core/navigation/block_runner.py`, `core_ui/pages.py`, `run_integrated.py`는 새 구성요소를 연결하는 범위만 수정한다.

---

### Task 1: 전역 좌표와 지도 데이터 모델

**Files:**
- Create: `core/navigation/world_map.py`
- Create: `tests/test_world_map.py`

**Interfaces:**
- Produces: `WorldPoint`, `Calibration`, `ActionSpec`, `NavNode`, `NavEdge`, `NavRoute`, `WorldMapModel`.
- Produces: `calibrate_two_points(world_a, world_b, local_a, local_b) -> Calibration`.
- Produces: `Calibration.local_to_world(point) -> WorldPoint`와 `Calibration.world_to_local(point) -> WorldPoint`.

- [ ] **Step 1: 두 점 보정과 데이터 검증 실패 테스트 작성**

```python
# tests/test_world_map.py
import math
import pytest

from core.navigation.world_map import (
    ActionSpec, NavEdge, NavNode, WorldPoint, calibrate_two_points,
)


def test_two_point_calibration_round_trip():
    cal = calibrate_two_points(
        WorldPoint(100, 50), WorldPoint(500, 250),
        WorldPoint(10, 5), WorldPoint(210, 105),
    )
    assert math.isclose(cal.scale, 2.0)
    assert cal.local_to_world(WorldPoint(10, 5)) == WorldPoint(100, 50)
    assert cal.world_to_local(WorldPoint(500, 250)) == WorldPoint(210, 105)


def test_calibration_rejects_short_or_rotated_pair():
    with pytest.raises(ValueError, match="기준점 거리"):
        calibrate_two_points(
            WorldPoint(1, 1), WorldPoint(2, 2),
            WorldPoint(1, 1), WorldPoint(1.1, 1.1),
        )
    with pytest.raises(ValueError, match="방향"):
        calibrate_two_points(
            WorldPoint(0, 0), WorldPoint(100, 0),
            WorldPoint(0, 0), WorldPoint(0, 100),
        )


def test_action_and_edge_validation():
    action = ActionSpec("up", 0.2, 1, 0.0, 1.0)
    node = NavNode("n1", "action", 20, 30, 4, "포탈", action)
    edge = NavEdge("e1", "n0", "n1", True, "walk")
    assert node.action.key == "up"
    assert edge.traversal == "walk"
    with pytest.raises(ValueError, match="repeat"):
        ActionSpec("up", 0.2, 0, 0.0, 1.0)
```

- [ ] **Step 2: 실패 상태 확인**

Run: `py -3.14 -m pytest tests/test_world_map.py -v`

Expected: `ModuleNotFoundError: No module named 'core.navigation.world_map'`.

- [ ] **Step 3: 최소 데이터 모델과 정·역변환 구현**

```python
# core/navigation/world_map.py 첫 구현의 공개부
# 큰 지도 절대좌표와 보정·이동 노드 데이터를 정의하는 순수 모델
from __future__ import annotations

import math
from dataclasses import dataclass, field


@dataclass(frozen=True)
class WorldPoint:
    x: float
    y: float


@dataclass(frozen=True)
class Calibration:
    scale: float
    offset_x: float
    offset_y: float

    def local_to_world(self, point: WorldPoint) -> WorldPoint:
        return WorldPoint(
            self.offset_x + point.x * self.scale,
            self.offset_y + point.y * self.scale,
        )

    def world_to_local(self, point: WorldPoint) -> WorldPoint:
        return WorldPoint(
            (point.x - self.offset_x) / self.scale,
            (point.y - self.offset_y) / self.scale,
        )


def calibrate_two_points(world_a: WorldPoint, world_b: WorldPoint,
                         local_a: WorldPoint, local_b: WorldPoint,
                         min_local_distance: float = 20.0,
                         max_angle_deg: float = 3.0) -> Calibration:
    wdx, wdy = world_b.x - world_a.x, world_b.y - world_a.y
    ldx, ldy = local_b.x - local_a.x, local_b.y - local_a.y
    wd, ld = math.hypot(wdx, wdy), math.hypot(ldx, ldy)
    if ld < min_local_distance or wd <= 0:
        raise ValueError("기준점 거리가 너무 짧습니다")
    cos_angle = (wdx * ldx + wdy * ldy) / (wd * ld)
    angle = math.degrees(math.acos(max(-1.0, min(1.0, cos_angle))))
    if angle > max_angle_deg:
        raise ValueError("두 좌표계의 기준점 방향이 다릅니다")
    scale = wd / ld
    return Calibration(
        scale=scale,
        offset_x=world_a.x - local_a.x * scale,
        offset_y=world_a.y - local_a.y * scale,
    )


@dataclass(frozen=True)
class ActionSpec:
    key: str
    hold_sec: float
    repeat: int
    repeat_interval_sec: float
    wait_after_sec: float

    def __post_init__(self) -> None:
        if not self.key.strip():
            raise ValueError("action key는 비어 있을 수 없습니다")
        if self.repeat < 1:
            raise ValueError("repeat는 1 이상이어야 합니다")
        if min(self.hold_sec, self.repeat_interval_sec, self.wait_after_sec) < 0:
            raise ValueError("action 시간은 음수일 수 없습니다")


@dataclass(frozen=True)
class NavNode:
    id: str
    kind: str
    x: float
    y: float
    arrival_radius: float = 4.0
    label: str = ""
    action: ActionSpec | None = None

    def __post_init__(self) -> None:
        if self.kind not in {"waypoint", "action"}:
            raise ValueError("node kind는 waypoint 또는 action이어야 합니다")
        if self.kind == "action" and self.action is None:
            raise ValueError("action 노드에는 action 설정이 필요합니다")


@dataclass(frozen=True)
class NavEdge:
    id: str
    from_id: str
    to_id: str
    bidirectional: bool
    traversal: str
    ladder: dict | None = None

    def __post_init__(self) -> None:
        if self.traversal not in {"walk", "teleport", "ladder"}:
            raise ValueError("지원하지 않는 traversal입니다")
        if self.traversal == "ladder" and not self.ladder:
            raise ValueError("ladder 연결에는 ladder 설정이 필요합니다")


@dataclass(frozen=True)
class NavRoute:
    id: str
    name: str
    node_ids: tuple[str, ...]
    loop: bool = True


@dataclass
class WorldMapModel:
    enabled: bool = False
    image_path: str = ""
    image_width: int = 0
    image_height: int = 0
    calibration: Calibration | None = None
    nodes: dict[str, NavNode] = field(default_factory=dict)
    edges: tuple[NavEdge, ...] = ()
    routes: dict[str, NavRoute] = field(default_factory=dict)
```

- [ ] **Step 4: 모델 테스트 통과 확인**

Run: `py -3.14 -m pytest tests/test_world_map.py -v`

Expected: `3 passed`.

- [ ] **Step 5: 모델 커밋**

```bash
git add core/navigation/world_map.py tests/test_world_map.py
git commit -m "전역 지도 좌표 모델 추가"
```

---

### Task 2: 미니맵 창 추적과 추정 위치

**Files:**
- Create: `core/navigation/viewport_tracker.py`
- Create: `tests/test_viewport_tracker.py`

**Interfaces:**
- Consumes: `WorldPoint`, `Calibration`.
- Produces: `TrackResult(origin, confidence, state)`.
- Produces: `ViewportTracker.update(frame_bgr, local_char) -> TrackResult`.
- Produces: `ViewportTracker.character_world(local_char) -> WorldPoint`.

- [ ] **Step 1: 확정·추정·복구 테스트 작성**

```python
# tests/test_viewport_tracker.py
import numpy as np

from core.navigation.viewport_tracker import ViewportTracker
from core.navigation.world_map import Calibration, WorldPoint


def test_confirmed_match_sets_world_origin():
    matches = iter([(WorldPoint(100, 40), 0.92, (0.0, 0.0, 1.0))])
    tracker = ViewportTracker(
        np.zeros((300, 600), dtype=np.uint8),
        Calibration(2.0, 0.0, 0.0),
        match_fn=lambda *_: next(matches),
    )
    result = tracker.update(np.zeros((50, 100, 3), dtype=np.uint8), WorldPoint(20, 10))
    assert result.state == "confirmed"
    assert tracker.character_world(WorldPoint(20, 10)) == WorldPoint(140, 60)


def test_failed_global_match_uses_frame_shift_without_timeout():
    matches = iter([
        (WorldPoint(100, 40), 0.92, (0.0, 0.0, 1.0)),
        (WorldPoint(0, 0), 0.10, (-3.0, 0.0, 0.8)),
        (WorldPoint(0, 0), 0.10, (0.0, 0.0, 0.0)),
    ])
    tracker = ViewportTracker(
        np.zeros((300, 600), dtype=np.uint8),
        Calibration(2.0, 0.0, 0.0),
        match_fn=lambda *_: next(matches),
    )
    frame = np.zeros((50, 100, 3), dtype=np.uint8)
    tracker.update(frame, WorldPoint(20, 10))
    assert tracker.update(frame, WorldPoint(20, 10)).origin == WorldPoint(106, 40)
    result = tracker.update(frame, WorldPoint(20, 10))
    assert result.origin == WorldPoint(112, 40)
    assert result.state == "estimated"
```

- [ ] **Step 2: 실패 상태 확인**

Run: `py -3.14 -m pytest tests/test_viewport_tracker.py -v`

Expected: 모듈 누락으로 실패.

- [ ] **Step 3: 주입 가능한 매칭기와 무제한 추정 구현**

```python
# core/navigation/viewport_tracker.py 공개부
# 미니맵 배경을 큰 지도에 맞춰 현재 창 원점과 추정 상태를 계산하는 추적기
from __future__ import annotations

from dataclasses import dataclass
from typing import Callable

import cv2
import numpy as np

from core.navigation.world_map import Calibration, WorldPoint


@dataclass(frozen=True)
class TrackResult:
    origin: WorldPoint
    confidence: float
    state: str


def _default_match(world_gray: np.ndarray, current_bgr: np.ndarray,
                   previous_bgr: np.ndarray | None, scale: float):
    current = cv2.cvtColor(current_bgr, cv2.COLOR_BGR2GRAY)
    scaled = cv2.resize(current, None, fx=scale, fy=scale,
                        interpolation=cv2.INTER_LINEAR)
    scores = cv2.matchTemplate(world_gray, scaled, cv2.TM_CCOEFF_NORMED)
    _, confidence, _, loc = cv2.minMaxLoc(scores)
    shift = (0.0, 0.0, 0.0)
    if previous_bgr is not None:
        previous = cv2.cvtColor(previous_bgr, cv2.COLOR_BGR2GRAY).astype(np.float32)
        now = current.astype(np.float32)
        if previous.shape == now.shape:
            (dx, dy), response = cv2.phaseCorrelate(previous, now)
            shift = (dx, dy, response)
    return WorldPoint(float(loc[0]), float(loc[1])), float(confidence), shift


class ViewportTracker:
    def __init__(self, world_gray: np.ndarray, calibration: Calibration,
                 match_fn: Callable | None = None,
                 confirm_threshold: float = 0.72,
                 phase_threshold: float = 0.20):
        self._world = world_gray
        self._cal = calibration
        self._match = match_fn or _default_match
        self._confirm_threshold = confirm_threshold
        self._phase_threshold = phase_threshold
        self._result: TrackResult | None = TrackResult(
            WorldPoint(calibration.offset_x, calibration.offset_y), 0.0, "estimated")
        self._previous: np.ndarray | None = None
        self._velocity = WorldPoint(0.0, 0.0)
        self._viewport_size = (0.0, 0.0)

    @staticmethod
    def _limited_correction(previous: WorldPoint, matched: WorldPoint,
                            max_step: float = 12.0) -> WorldPoint:
        dx, dy = matched.x - previous.x, matched.y - previous.y
        distance = float(np.hypot(dx, dy))
        if distance <= max_step or distance == 0:
            return matched
        ratio = max_step / distance
        return WorldPoint(previous.x + dx * ratio, previous.y + dy * ratio)

    def update(self, frame_bgr: np.ndarray, local_char: WorldPoint) -> TrackResult:
        height, width = frame_bgr.shape[:2]
        self._viewport_size = (width * self._cal.scale, height * self._cal.scale)
        matched, confidence, shift = self._match(
            self._world, frame_bgr, self._previous, self._cal.scale)
        previous_origin = self._result.origin
        if confidence >= self._confirm_threshold:
            origin = self._limited_correction(previous_origin, matched)
            state = "confirmed"
        else:
            dx, dy, response = shift
            if response >= self._phase_threshold:
                origin = WorldPoint(
                    previous_origin.x - dx * self._cal.scale,
                    previous_origin.y - dy * self._cal.scale,
                )
            else:
                origin = WorldPoint(
                    previous_origin.x + self._velocity.x,
                    previous_origin.y + self._velocity.y,
                )
            state = "estimated"
        self._velocity = WorldPoint(
            origin.x - previous_origin.x, origin.y - previous_origin.y)
        self._previous = frame_bgr.copy()
        self._result = TrackResult(origin, confidence, state)
        return self._result

    def character_world(self, local_char: WorldPoint) -> WorldPoint:
        if self._result is None:
            raise RuntimeError("전역 위치가 아직 초기화되지 않았습니다")
        return WorldPoint(
            self._result.origin.x + local_char.x * self._cal.scale,
            self._result.origin.y + local_char.y * self._cal.scale,
        )

    @property
    def result(self) -> TrackResult | None:
        return self._result

    @property
    def viewport_size(self) -> tuple[float, float]:
        return self._viewport_size
```

- [ ] **Step 4: 추적기 테스트 통과 확인**

Run: `py -3.14 -m pytest tests/test_viewport_tracker.py -v`

Expected: `2 passed`.

- [ ] **Step 5: 추적기 커밋**

```bash
git add core/navigation/viewport_tracker.py tests/test_viewport_tracker.py
git commit -m "미니맵 창 전역 위치 추적기 추가"
```

---

### Task 3: 전역 연결망과 제한형 액션 실행

**Files:**
- Create: `core/navigation/world_graph.py`
- Create: `core/navigation/world_runner.py`
- Modify: `core/navigation/block_runner.py`
- Create: `tests/test_world_graph.py`
- Create: `tests/test_world_runner.py`
- Modify: `tests/test_block_runner.py`

**Interfaces:**
- Consumes: `NavNode`, `NavEdge`, `NavRoute`, `ActionSpec`.
- Produces: `shortest_edge_path(edges, start_id, goal_id) -> list[NavEdge] | None`.
- Produces: `ActionExecutor.execute(spec) -> None`.
- Produces: `WorldRouteRunner.run_node_path(node_ids) -> bool`와 `WorldRouteRunner.navigate_to(start_id, goal_id) -> bool`.
- Changes: `BlockRunner.run_block(block, max_steps=200, arrival_tolerance=None) -> bool`.

- [ ] **Step 1: 최단경로·도착오차·액션 테스트 작성**

```python
# tests/test_world_graph.py
from core.navigation.world_graph import shortest_edge_path
from core.navigation.world_map import NavEdge


def test_shortest_path_uses_registered_edges_only():
    edges = (
        NavEdge("e1", "a", "b", True, "walk"),
        NavEdge("e2", "b", "c", True, "ladder", {"x": 50}),
    )
    assert [e.id for e in shortest_edge_path(edges, "a", "c")] == ["e1", "e2"]
    assert shortest_edge_path(edges, "a", "outside") is None


def test_reverse_ladder_path_flips_direction():
    edge = NavEdge("e1", "low", "high", True, "ladder", {
        "x": 50, "y_top": 20, "y_bot": 80, "direction": "up",
        "exit_side": "left", "grab_side": "auto", "jump_offset": 8,
    })
    reverse = shortest_edge_path((edge,), "high", "low")[0]
    assert (reverse.from_id, reverse.to_id) == ("high", "low")
    assert reverse.ladder["direction"] == "down"
```

```python
# tests/test_world_runner.py
from core.navigation.world_map import ActionSpec
from core.navigation.world_runner import ActionExecutor


class FakeHumanizer:
    def __init__(self): self.intents = []
    def perform(self, intent): self.intents.append(intent)


def test_action_executor_repeats_through_humanizer():
    h = FakeHumanizer()
    sleeps = []
    executor = ActionExecutor(h, sleep_fn=sleeps.append)
    executor.execute(ActionSpec("up", 0.2, 2, 0.3, 1.0))
    assert [i.key for i in h.intents] == ["up", "up"]
    assert sleeps == [0.3, 1.0]
```

```python
# tests/test_block_runner.py 추가
def test_custom_arrival_tolerance():
    h = FakeHumanizer()
    runner = BlockRunner(humanizer=h, pos_fn=lambda: (96, 10))
    assert runner.run_block(
        Block(type="move", target_x=100), max_steps=1, arrival_tolerance=5
    ) is True
```

- [ ] **Step 2: 실패 상태 확인**

Run: `py -3.14 -m pytest tests/test_world_graph.py tests/test_world_runner.py tests/test_block_runner.py::test_custom_arrival_tolerance -v`

Expected: 새 모듈 누락과 `arrival_tolerance` 인자 오류로 실패.

- [ ] **Step 3: 그래프·액션·도착오차 구현**

```python
# core/navigation/world_graph.py
# 등록된 전역 이동 연결만 사용해 노드 간 최단경로를 계산하는 순수 그래프
from collections import deque
from dataclasses import replace


def _reverse_edge(edge):
    ladder = dict(edge.ladder) if edge.ladder else None
    if ladder and "direction" in ladder:
        ladder["direction"] = "down" if ladder["direction"] == "up" else "up"
    return replace(edge, from_id=edge.to_id, to_id=edge.from_id, ladder=ladder)


def shortest_edge_path(edges, start_id: str, goal_id: str):
    if start_id == goal_id:
        return []
    graph = {}
    for edge in edges:
        graph.setdefault(edge.from_id, []).append(edge)
        if edge.bidirectional:
            graph.setdefault(edge.to_id, []).append(_reverse_edge(edge))
    queue = deque([(start_id, [])])
    seen = {start_id}
    while queue:
        node_id, path = queue.popleft()
        for edge in graph.get(node_id, []):
            next_id = edge.to_id
            if next_id in seen:
                continue
            next_path = path + [edge]
            if next_id == goal_id:
                return next_path
            seen.add(next_id)
            queue.append((next_id, next_path))
    return None
```

```python
# core/navigation/world_runner.py 핵심 공개부
# 전역 노드 경로를 기존 이동기와 제한형 키 액션으로 실행하는 조정기
import time

from core.humanize.intent import Intent
from core.navigation.block import Block
from core.navigation.world_graph import shortest_edge_path


class ActionExecutor:
    def __init__(self, humanizer, sleep_fn=time.sleep):
        self._humanizer = humanizer
        self._sleep = sleep_fn

    def execute(self, spec) -> None:
        for index in range(spec.repeat):
            self._humanizer.perform(Intent(
                action="key", key=spec.key, base_hold_sec=spec.hold_sec))
            if index + 1 < spec.repeat and spec.repeat_interval_sec > 0:
                self._sleep(spec.repeat_interval_sec)
        if spec.wait_after_sec > 0:
            self._sleep(spec.wait_after_sec)


class WorldRouteRunner:
    def __init__(self, model, block_runner, action_executor):
        self._model = model
        self._blocks = block_runner
        self._actions = action_executor

    def _run_edge(self, edge, target) -> bool:
        if edge.traversal in {"walk", "teleport"}:
            block = Block(type="move", target_x=target.x,
                          move_type=edge.traversal, pos_x=int(target.x), pos_y=int(target.y))
        else:
            data = dict(edge.ladder or {})
            block = Block(type="ladder", ladder_x=int(data["x"]),
                          y_top=int(data["y_top"]), y_bot=int(data["y_bot"]),
                          ladder_dir=data["direction"], exit_side=data["exit_side"],
                          grab_side=data["grab_side"], jump_offset=int(data["jump_offset"]))
        return self._blocks.run_block(
            block, arrival_tolerance=target.arrival_radius)

    def run_node_path(self, node_ids) -> bool:
        for left, right in zip(node_ids, node_ids[1:]):
            edges = shortest_edge_path(self._model.edges, left, right)
            if edges is None:
                return False
            for edge in edges:
                target_id = edge.to_id
                target = self._model.nodes[target_id]
                if not self._run_edge(edge, target):
                    return False
                left = target_id
                if target.kind == "action":
                    self._actions.execute(target.action)
        return True

    def navigate_to(self, start_id: str, goal_id: str) -> bool:
        path = shortest_edge_path(self._model.edges, start_id, goal_id)
        if path is None:
            return False
        node_ids = [start_id]
        current = start_id
        for edge in path:
            current = edge.to_id if edge.from_id == current else edge.from_id
            node_ids.append(current)
        return self.run_node_path(node_ids)
```

`BlockRunner.run_block`은 `arrival_tolerance`를 `_exec_move`로 전달하고, `_exec_move`의 도착 판정은 `tol = TOLERANCE if arrival_tolerance is None else arrival_tolerance`를 사용한다. 기존 호출은 인자를 생략하므로 동작이 바뀌지 않는다.

- [ ] **Step 4: 관련 테스트 통과 확인**

Run: `py -3.14 -m pytest tests/test_world_graph.py tests/test_world_runner.py tests/test_block_runner.py -v`

Expected: 전역 신규 테스트와 기존 이동기 테스트 전체 통과.

- [ ] **Step 5: 경로·액션 커밋**

```bash
git add core/navigation/world_graph.py core/navigation/world_runner.py core/navigation/block_runner.py tests/test_world_graph.py tests/test_world_runner.py tests/test_block_runner.py
git commit -m "전역 경로와 제한형 액션 실행 추가"
```

---

### Task 4: 설정 저장과 기존 좌표 자동 변환

**Files:**
- Modify: `core/config_manager.py`
- Modify: `core/config_adapter.py`
- Modify: `core/runtime.py`
- Create: `core/navigation/world_migration.py`
- Create: `tests/test_world_migration.py`
- Modify: `tests/test_config_adapter.py`

**Interfaces:**
- Produces: `WorldMapModel.from_dict(data) -> WorldMapModel`와 `to_dict()`.
- Produces: `backup_config(config_path, timestamp) -> Path`.
- Produces: `migrate_legacy_data(data, calibration, viewport_origin) -> dict`.
- Changes: `RuntimeConfig.world_map: WorldMapModel | None`.

- [ ] **Step 1: 설정 매핑과 일회성 변환 테스트 작성**

```python
# tests/test_world_migration.py
from copy import deepcopy

from core.navigation.world_map import Calibration, WorldPoint
from core.navigation.world_migration import migrate_legacy_data


def test_migrates_route_and_zone_once():
    source = {
        "floor_hunt": {"route": [{"type": "move", "pos_x": 10, "pos_y": 20,
                                    "start_x": 5, "end_x": 25}]},
        "zones": [{"name": "1층", "left_x": 5, "right_x": 25,
                   "y_min": 18, "y_max": 22}],
        "world_map": {"migration_completed": False},
    }
    result = migrate_legacy_data(
        deepcopy(source), Calibration(2.0, 0.0, 0.0), WorldPoint(100, 40))
    block = result["floor_hunt"]["route"][0]
    assert (block["pos_x"], block["pos_y"]) == (120, 80)
    assert result["world_map"]["migration_completed"] is True
    assert migrate_legacy_data(deepcopy(result), Calibration(2, 0, 0), WorldPoint(0, 0)) == result
```

```python
# tests/test_config_adapter.py 추가
def test_maps_enabled_world_map():
    data = {
        "world_map": {
            "enabled": True,
            "image_path": "maps/test.png",
            "image_width": 1000,
            "image_height": 500,
            "calibration": {"scale": 2.0, "offset": [10.0, 20.0]},
            "tracking_policy": "continue_estimated",
        },
        "navigation": {"nodes": [], "edges": [], "routes": []},
    }
    runtime = to_runtime_config(data)
    assert runtime.world_map.enabled is True
    assert runtime.world_map.calibration.scale == 2.0
```

- [ ] **Step 2: 실패 상태 확인**

Run: `py -3.14 -m pytest tests/test_world_migration.py tests/test_config_adapter.py -v`

Expected: 마이그레이션 모듈과 `RuntimeConfig.world_map` 누락으로 실패.

- [ ] **Step 3: 기본 설정·직렬화·백업·변환 구현**

`DEFAULT_CONFIG`에 아래 값을 추가한다.

```python
"world_map": {
    "enabled": False,
    "image_path": "",
    "image_width": 0,
    "image_height": 0,
    "calibration": None,
    "tracking_policy": "continue_estimated",
    "migration_completed": False,
    "legacy_backup_path": "",
},
"navigation": {"nodes": [], "edges": [], "routes": []},
```

```python
# core/navigation/world_migration.py 공개부
# 기존 미니맵 로컬 설정을 백업하고 큰 지도 절대좌표로 한 번만 변환한다
from __future__ import annotations

import json
import shutil
from copy import deepcopy
from pathlib import Path

from core.navigation.world_map import WorldPoint


def backup_config(config_path: str, timestamp: str) -> Path:
    source = Path(config_path)
    target = source.with_name(f"config.pre_world_map_{timestamp}.json")
    shutil.copy2(source, target)
    return target


def migrate_legacy_data(data: dict, calibration, viewport_origin: WorldPoint) -> dict:
    result = deepcopy(data)
    world = result.setdefault("world_map", {})
    if world.get("migration_completed"):
        return result

    def point(x, y):
        return WorldPoint(
            viewport_origin.x + float(x) * calibration.scale,
            viewport_origin.y + float(y) * calibration.scale,
        )

    for block in result.get("floor_hunt", {}).get("route", []):
        if int(block.get("pos_x", -1)) >= 0 and int(block.get("pos_y", -1)) >= 0:
            p = point(block["pos_x"], block["pos_y"])
            block["pos_x"], block["pos_y"] = round(p.x), round(p.y)
        for key in ("target_x", "start_x", "end_x", "ladder_x"):
            if key in block:
                block[key] = round(viewport_origin.x + float(block[key]) * calibration.scale)
        for key in ("y_top", "y_bot"):
            if key in block:
                block[key] = round(viewport_origin.y + float(block[key]) * calibration.scale)
    for zone in result.get("zones", []):
        zone["left_x"] = round(viewport_origin.x + float(zone["left_x"]) * calibration.scale)
        zone["right_x"] = round(viewport_origin.x + float(zone["right_x"]) * calibration.scale)
        zone["y_min"] = round(viewport_origin.y + float(zone["y_min"]) * calibration.scale)
        zone["y_max"] = round(viewport_origin.y + float(zone["y_max"]) * calibration.scale)
    world["migration_completed"] = True
    return result
```

`WorldMapModel.from_dict`는 `world_map`과 `navigation`을 함께 받아 모델을 구성하고 `to_runtime_config`는 유효한 모델만 `RuntimeConfig.world_map`에 넣는다. 이미지 경로 또는 보정값이 없으면 `world_map=None`으로 내려 기존 경로를 사용한다.

- [ ] **Step 4: 설정·변환 테스트 통과 확인**

Run: `py -3.14 -m pytest tests/test_world_migration.py tests/test_config_adapter.py -v`

Expected: 신규 테스트와 기존 설정 어댑터 테스트 전체 통과.

- [ ] **Step 5: 설정·마이그레이션 커밋**

```bash
git add core/config_manager.py core/config_adapter.py core/runtime.py core/navigation/world_map.py core/navigation/world_migration.py tests/test_world_migration.py tests/test_config_adapter.py
git commit -m "전역 지도 설정과 기존 좌표 변환 추가"
```

---

### Task 5: 전역 위치 스캐너와 런타임 실행 연결

**Files:**
- Create: `core/sensing/world_position_scanner.py`
- Modify: `core/runtime.py`
- Create: `tests/test_world_position_scanner.py`
- Modify: `tests/test_runtime.py`

**Interfaces:**
- Consumes: 미니맵 캡처 함수, `ViewportTracker`, 기존 Orchestrator 로컬 위치.
- Produces: `WorldPositionScanner.start()`, `stop()`, `scan_once()`.
- Produces: `BotRuntime.world_position() -> tuple[float, float] | None`.
- Produces: `BotRuntime.world_tracking_state() -> str`와 `BotRuntime.world_viewport() -> tuple[tuple[float, float], tuple[int, int]] | None`.
- Produces: `BotRuntime.start_world_route(route_id) -> bool`와 `BotRuntime.navigate_world_to(node_id) -> bool`.

- [ ] **Step 1: 스캐너와 런타임 비활성 폴백 테스트 작성**

```python
# tests/test_world_position_scanner.py
import numpy as np

from core.navigation.world_map import WorldPoint
from core.sensing.world_position_scanner import WorldPositionScanner


class FakeTracker:
    def update(self, frame, local): return type("R", (), {"state": "confirmed"})()
    def character_world(self, local): return WorldPoint(local.x + 100, local.y + 40)


def test_scan_once_updates_world_position():
    scanner = WorldPositionScanner(
        capture_fn=lambda region: np.zeros((20, 40, 3), dtype=np.uint8),
        region_fn=lambda: {"left": 0, "top": 0, "width": 40, "height": 20},
        local_position_fn=lambda: (10, 5),
        tracker=FakeTracker(),
        interval_sec=0.01,
    )
    scanner.scan_once()
    assert scanner.position() == (110, 45)
    assert scanner.state() == "confirmed"
```

```python
# tests/test_runtime.py 추가
from core.runtime import BotRuntime


def test_world_navigation_is_absent_when_world_map_disabled():
    runtime = object.__new__(BotRuntime)
    runtime._world_scanner = None
    runtime._world_runner = None
    assert runtime.world_position() is None
    assert runtime.start_world_route("missing") is False
    assert runtime.navigate_world_to("missing") is False
```

- [ ] **Step 2: 실패 상태 확인**

Run: `py -3.14 -m pytest tests/test_world_position_scanner.py tests/test_runtime.py -v`

Expected: 스캐너 모듈과 런타임 공개 메서드 누락으로 실패.

- [ ] **Step 3: 스캐너와 전역 런타임 조립 구현**

```python
# core/sensing/world_position_scanner.py 공개부
# 미니맵을 주기 캡처해 전역 캐릭터 위치와 추적 상태를 공유하는 스캐너
from __future__ import annotations

import threading
import time

from core.navigation.world_map import WorldPoint


class WorldPositionScanner:
    def __init__(self, capture_fn, region_fn, local_position_fn, tracker,
                 interval_sec: float = 0.08):
        self._capture = capture_fn
        self._region = region_fn
        self._local = local_position_fn
        self._tracker = tracker
        self._interval = interval_sec
        self._position = None
        self._state = "unavailable"
        self._stop = threading.Event()
        self._thread = None

    def scan_once(self):
        local = self._local()
        if local is None:
            return None
        frame = self._capture(self._region())
        result = self._tracker.update(frame, WorldPoint(*local))
        point = self._tracker.character_world(WorldPoint(*local))
        self._position = (point.x, point.y)
        self._state = result.state
        return self._position

    def start(self):
        if self._thread and self._thread.is_alive():
            return
        self._stop.clear()
        self._thread = threading.Thread(target=self._loop, daemon=True,
                                        name="WorldPositionScanner")
        self._thread.start()

    def _loop(self):
        while not self._stop.is_set():
            try:
                self.scan_once()
            except Exception:
                self._state = "estimated"
            self._stop.wait(self._interval)

    def stop(self):
        self._stop.set()

    def position(self): return self._position
    def state(self): return self._state
    def viewport(self):
        result = self._tracker.result
        if result is None:
            return None
        return ((result.origin.x, result.origin.y), self._tracker.viewport_size)
```

`BotRuntime`는 `config.world_map`이 있을 때만 지도 이미지를 회색조로 로드하고 `ViewportTracker`, `WorldPositionScanner`, 전역 좌표용 `BlockRunner`, `ActionExecutor`, `WorldRouteRunner`를 조립해 각각 `self._world_scanner`와 `self._world_runner`에 저장한다. 전역 좌표용 `BlockRunner.pos_fn`은 `self.world_position() or (0, 0)`을 사용한다. `start_scanners`와 `stop_scanners`에서 전역 스캐너도 함께 시작·정지한다. 전역 모드가 꺼져 있으면 두 속성은 `None`이며 공개 메서드는 위치에 `None`, 실행 요청에 `False`를 반환한다.

```python
# core/runtime.py에 추가할 전역 실행 공개부
def world_position(self):
    return self._world_scanner.position() if self._world_scanner else None

def world_tracking_state(self):
    return self._world_scanner.state() if self._world_scanner else "unavailable"

def world_viewport(self):
    return self._world_scanner.viewport() if self._world_scanner else None

def _start_world_job(self, fn) -> bool:
    import threading
    if self._world_runner is None:
        return False
    if getattr(self, "_world_thread", None) and self._world_thread.is_alive():
        return False
    self._world_thread = threading.Thread(target=fn, daemon=True,
                                          name="WorldNavigation")
    self._world_thread.start()
    return True

def start_world_route(self, route_id: str) -> bool:
    if self._world_runner is None:
        return False
    route = self._cfg.world_map.routes.get(route_id)
    if route is None:
        return False
    def run():
        while self._route_can_run():
            if not self._world_runner.run_node_path(route.node_ids):
                return
            if not route.loop:
                return
    return self._start_world_job(run)

def navigate_world_to(self, node_id: str) -> bool:
    if self._world_runner is None or node_id not in self._cfg.world_map.nodes:
        return False
    position = self.world_position()
    if position is None:
        return False
    px, py = position
    start = min(
        self._cfg.world_map.nodes.values(),
        key=lambda node: (node.x - px) ** 2 + (node.y - py) ** 2,
    )
    return self._start_world_job(
        lambda: self._world_runner.navigate_to(start.id, node_id))
```

반복 루트는 기존 `FloorHuntRunner`와 충돌하지 않게 한 번에 하나만 활성화한다. `start_world_route`는 별도 데몬 스레드에서 route의 `node_ids`를 실행하고 `loop`가 참이면 `_route_can_run()` 동안 반복한다. `navigate_world_to`는 현재 위치에서 가장 가까운 노드를 시작점으로 선택한 뒤 일회성 데몬 스레드에서 `WorldRouteRunner.navigate_to`를 실행한다.

- [ ] **Step 4: 런타임 관련 테스트 통과 확인**

Run: `py -3.14 -m pytest tests/test_world_position_scanner.py tests/test_runtime.py tests/test_route_attack.py -v`

Expected: 신규 전역 런타임 테스트와 기존 사냥·공격 테스트 전체 통과.

- [ ] **Step 5: 런타임 커밋**

```bash
git add core/sensing/world_position_scanner.py core/runtime.py tests/test_world_position_scanner.py tests/test_runtime.py
git commit -m "전역 위치 스캐너와 런타임 연결"
```

---

### Task 6: 큰 지도 캔버스와 편집 UI

**Files:**
- Create: `core_ui/world_map_canvas.py`
- Create: `core_ui/world_map_editor.py`
- Create: `tests/test_world_map_canvas.py`
- Create: `tests/test_world_map_editor.py`

**Interfaces:**
- Produces: `WorldMapCanvas.set_tool(tool)`, `set_viewport(origin, size, state)`, `set_character(position)`, `selected_world_point` signal.
- Produces: `WorldMapEditor.destination_requested(str)`와 `route_start_requested(str)` 신호.
- Consumes: `ConfigManager`의 `world_map`과 `navigation`.
- Consumes: `screen_capture(region)`로 현재 미니맵 기준점 선택 화면을 갱신한다.

- [ ] **Step 1: 오프스크린 좌표 선택과 설정 저장 테스트 작성**

```python
# tests/test_world_map_canvas.py
import os
os.environ.setdefault("QT_QPA_PLATFORM", "offscreen")

from PyQt6.QtCore import QPoint
from PyQt6.QtWidgets import QApplication
from core_ui.world_map_canvas import WorldMapCanvas


def test_canvas_click_emits_world_coordinate():
    app = QApplication.instance() or QApplication([])
    canvas = WorldMapCanvas()
    canvas.resize(400, 200)
    canvas.set_world_size(800, 400)
    points = []
    canvas.selected_world_point.connect(lambda x, y: points.append((x, y)))
    canvas.select_canvas_point(QPoint(200, 100))
    assert points == [(400.0, 200.0)]
```

```python
# tests/test_world_map_editor.py
from PyQt6.QtWidgets import QApplication

from core_ui.world_map_editor import WorldMapEditor


class FakeConfig:
    def __init__(self):
        self.data = {"navigation": {"nodes": [], "edges": [], "routes": []}}
        self.saved = 0

    def get(self, *keys, default=None):
        node = self.data
        for key in keys:
            if not isinstance(node, dict) or key not in node:
                return default
            node = node[key]
        return node

    def set(self, *args):
        *keys, value = args
        node = self.data
        for key in keys[:-1]:
            node = node.setdefault(key, {})
        node[keys[-1]] = value

    def save(self):
        self.saved += 1


def test_editor_saves_action_node():
    app = QApplication.instance() or QApplication([])
    config = FakeConfig()
    editor = WorldMapEditor(config)
    editor.add_action_node(100, 80, "포탈", "up", 0.2, 1, 0.0, 1.0)
    nodes = config.get("navigation", "nodes")
    assert nodes[0]["kind"] == "action"
    assert nodes[0]["action"]["key"] == "up"
    assert config.saved == 1
```

- [ ] **Step 2: 실패 상태 확인**

Run: `py -3.14 -m pytest tests/test_world_map_canvas.py tests/test_world_map_editor.py -v`

Expected: 두 UI 모듈 누락으로 실패.

- [ ] **Step 3: 최소 캔버스와 편집기 구현**

```python
# core_ui/world_map_canvas.py 공개부
# 큰 지도 위에 현재 미니맵 창·영역·노드·연결을 표시하고 좌표를 선택하는 캔버스
from PyQt6.QtCore import QTimer, pyqtSignal
from PyQt6.QtGui import QPainter, QPixmap
from PyQt6.QtWidgets import QWidget


class WorldMapCanvas(QWidget):
    selected_world_point = pyqtSignal(float, float)

    def __init__(self, parent=None):
        super().__init__(parent)
        self._pixmap = QPixmap()
        self._world_size = (0, 0)
        self._tool = None
        self._viewport = None
        self._character = None
        self._nodes = []
        self._edges = []
        self._zones = []
        self.setMinimumHeight(360)

    def set_image(self, path: str):
        self._pixmap = QPixmap(path)
        self.set_world_size(self._pixmap.width(), self._pixmap.height())
        self.update()

    def set_world_size(self, width: int, height: int):
        self._world_size = (width, height)

    def set_tool(self, tool: str | None): self._tool = tool
    def set_data(self, nodes, edges, zones):
        self._nodes, self._edges, self._zones = nodes, edges, zones
        self.update()
    def set_viewport(self, origin, size, state):
        self._viewport = (origin, size, state)
        self.update()
    def set_character(self, position):
        self._character = position
        self.update()

    def select_canvas_point(self, point):
        ww, wh = self._world_size
        if ww <= 0 or wh <= 0 or self.width() <= 0 or self.height() <= 0:
            return
        self.selected_world_point.emit(
            point.x() * ww / self.width(), point.y() * wh / self.height())

    def mousePressEvent(self, event):
        self.select_canvas_point(event.position().toPoint())

    def paintEvent(self, event):
        painter = QPainter(self)
        if not self._pixmap.isNull():
            painter.drawPixmap(self.rect(), self._pixmap)
        self._draw_zones(painter)
        self._draw_edges(painter)
        self._draw_nodes(painter)
        self._draw_viewport(painter)
```

`_draw_zones`, `_draw_edges`, `_draw_nodes`, `_draw_viewport`는 모두 큰 지도 좌표를 현재 위젯 배율로 바꿔 그린다. `confirmed` 창은 실선, `estimated` 창은 점선으로 표시한다.

```python
# core_ui/world_map_editor.py 공개 인터페이스
# 큰 지도 불러오기·두 점 보정·노드·연결·루트·목적지 요청을 묶는 편집 위젯
from PyQt6.QtCore import pyqtSignal
from PyQt6.QtWidgets import QWidget

from core.navigation.world_map import WorldPoint, calibrate_two_points


class WorldMapEditor(QWidget):
    destination_requested = pyqtSignal(str)
    route_start_requested = pyqtSignal(str)

    def __init__(self, config, screen_capture=None, parent=None):
        super().__init__(parent)
        self.setObjectName("worldMapEditor")
        self._cfg = config
        self._capture = screen_capture
        self._world_points = []
        self._local_points = []
        self._runtime_timer = QTimer(self)
        self._runtime_timer.timeout.connect(self._refresh_runtime)
        self._build_ui()
        self.reload()

    def apply_calibration(self):
        calibration = calibrate_two_points(
            self._world_points[0], self._world_points[1],
            self._local_points[0], self._local_points[1])
        self._cfg.set("world_map", "calibration", {
            "scale": calibration.scale,
            "offset": [calibration.offset_x, calibration.offset_y],
        })
        self._backup_and_migrate_once(calibration)
        self._cfg.save()
        return calibration

    def add_action_node(self, x, y, label, key, hold_sec, repeat,
                        repeat_interval_sec, wait_after_sec):
        nodes = list(self._cfg.get("navigation", "nodes", default=[]) or [])
        nodes.append({
            "id": f"node-{len(nodes) + 1:03d}", "kind": "action",
            "x": float(x), "y": float(y), "arrival_radius": 4.0,
            "label": label,
            "action": {"key": key, "hold_sec": hold_sec, "repeat": repeat,
                       "repeat_interval_sec": repeat_interval_sec,
                       "wait_after_sec": wait_after_sec},
        })
        self._cfg.set("navigation", "nodes", nodes)
        self._cfg.save()
        self.reload()

    def set_runtime_state_provider(self, position_fn, tracking_state_fn, viewport_fn):
        self._position_fn = position_fn
        self._tracking_state_fn = tracking_state_fn
        self._viewport_fn = viewport_fn
        self._runtime_timer.start(100)

    def _refresh_runtime(self):
        viewport = self._viewport_fn()
        if viewport is None:
            return
        origin, size = viewport
        self._canvas.set_viewport(origin, size, self._tracking_state_fn())
        self._canvas.set_character(self._position_fn())
```

`_build_ui`는 왼쪽에 큰 지도 캔버스, 오른쪽에 현재 미니맵 캡처 패널을 배치한다. `기준점 A`와 `기준점 B` 단계에서는 먼저 큰 지도 지점을 클릭하고 이어서 같은 미니맵 지점을 클릭해야 다음 단계로 넘어간다. 그 아래에 `선택·사냥영역·이동점·액션점·연결` 도구, 노드 목록, 루트 목록, `목적지 이동`, `반복 루트 시작` 버튼을 만든다. 편집 이벤트마다 ConfigManager에 저장하고 캔버스를 다시 그린다.

`_backup_and_migrate_once`는 `world_map.migration_completed`가 거짓이고 현재 `CONFIG_PATH`가 존재할 때 `backup_config(CONFIG_PATH, time.strftime("%Y%m%d_%H%M%S"))`를 먼저 호출한다. 이후 `migrate_legacy_data`에 `WorldPoint(calibration.offset_x, calibration.offset_y)`를 현재 창 원점으로 전달하고, 성공한 새 데이터와 백업 경로를 ConfigManager에 반영한다. 백업 또는 변환 중 예외가 발생하면 기존 `_data`를 유지하고 보정 저장도 취소한다.

- [ ] **Step 4: UI 테스트 통과 확인**

Run: `py -3.14 -m pytest tests/test_world_map_canvas.py tests/test_world_map_editor.py -v`

Expected: 오프스크린 UI 테스트 전체 통과.

- [ ] **Step 5: UI 커밋**

```bash
git add core_ui/world_map_canvas.py core_ui/world_map_editor.py tests/test_world_map_canvas.py tests/test_world_map_editor.py
git commit -m "큰 지도 편집 캔버스 추가"
```

---

### Task 7: 기존 페이지·실행 진입점 통합

**Files:**
- Modify: `core_ui/pages.py`
- Modify: `run_integrated.py`
- Modify: `core_ui/minimap_canvas.py`
- Modify: `tests/test_route_canvas.py`
- Create: `tests/test_world_map_integration.py`

**Interfaces:**
- Consumes: `WorldMapEditor.destination_requested`, `route_start_requested`.
- Consumes: `BotRuntime.navigate_world_to`, `start_world_route`, `world_position`.
- Keeps: 기존 `RouteCanvas`와 `BlockEditor`를 전역 지도 미설정 시 그대로 사용.

- [ ] **Step 1: 모드 전환과 실행 신호 배선 테스트 작성**

```python
# tests/test_world_map_integration.py
from PyQt6.QtCore import QObject, pyqtSignal
from PyQt6.QtWidgets import QApplication

from core_ui.pages import build_pages
from core_ui.world_map_editor import WorldMapEditor
from run_integrated import bind_world_editor


class FakeConfig:
    def __init__(self): self.data = {"world_map": {"enabled": True}}
    def get(self, *keys, default=None):
        node = self.data
        for key in keys:
            if not isinstance(node, dict) or key not in node:
                return default
            node = node[key]
        return node
    def set(self, *args):
        *keys, value = args
        node = self.data
        for key in keys[:-1]: node = node.setdefault(key, {})
        node[keys[-1]] = value
    def save(self): pass


class FakeEditor(QObject):
    destination_requested = pyqtSignal(str)
    route_start_requested = pyqtSignal(str)
    def set_runtime_state_provider(self, **providers): self.providers = providers


class FakeShell:
    def __init__(self, editor): self.editor = editor
    def findChild(self, *_args): return self.editor


class FakeRuntime:
    def __init__(self): self.destinations, self.routes = [], []
    def navigate_world_to(self, node_id): self.destinations.append(node_id); return True
    def start_world_route(self, route_id): self.routes.append(route_id); return True
    def world_position(self): return None
    def world_tracking_state(self): return "unavailable"
    def world_viewport(self): return None


def test_navigation_page_contains_world_editor_when_enabled():
    app = QApplication.instance() or QApplication([])
    pages = build_pages(FakeConfig())
    editor = pages[1].findChild(WorldMapEditor, "worldMapEditor")
    assert editor is not None


def test_editor_requests_are_forwarded_to_runtime():
    app = QApplication.instance() or QApplication([])
    editor, runtime = FakeEditor(), FakeRuntime()
    bind_world_editor(FakeShell(editor), runtime)
    editor.destination_requested.emit("node-002")
    editor.route_start_requested.emit("route-001")
    assert runtime.destinations == ["node-002"]
    assert runtime.routes == ["route-001"]
    assert editor.providers["tracking_state_fn"]() == "unavailable"
```

- [ ] **Step 2: 실패 상태 확인**

Run: `py -3.14 -m pytest tests/test_world_map_integration.py tests/test_route_canvas.py -v`

Expected: `WorldMapEditor` 페이지와 `bind_world_editor` 누락으로 실패.

- [ ] **Step 3: 동선 페이지와 런타임 배선 구현**

`core_ui/pages.py`의 동선·이동 페이지에서 아래처럼 `WorldMapEditor`를 `nav_extras`의 첫 요소로 추가한다. `world_map.enabled`가 거짓이면 편집기는 지도 등록 안내만 표시하고 기존 `RouteCanvas`와 `BlockEditor`를 계속 노출한다. 참이면 큰 지도 편집기를 주 편집 영역으로 사용하되 기존 블록 목록은 변환 확인용으로 아래에 유지한다.

```python
from core_ui.world_map_editor import WorldMapEditor

try:
    from core.screen_reader import ScreenReader
    world_capture = ScreenReader().capture
except Exception:
    world_capture = None
world_editor = WorldMapEditor(c, screen_capture=world_capture)
nav_extras.append(world_editor)
```

```python
# run_integrated.py에 추가할 배선 함수
def bind_world_editor(shell, runtime):
    from core_ui.world_map_editor import WorldMapEditor
    editor = shell.findChild(WorldMapEditor, "worldMapEditor")
    if editor is None:
        return None
    editor.destination_requested.connect(runtime.navigate_world_to)
    editor.route_start_requested.connect(runtime.start_world_route)
    editor.set_runtime_state_provider(
        position_fn=runtime.world_position,
        tracking_state_fn=runtime.world_tracking_state,
        viewport_fn=runtime.world_viewport,
    )
    return editor
```

`main()`은 `rt, rc, cm = build_runtime()`과 `MainShell(config=cm)` 생성 후 `bind_world_editor(shell, rt)`를 호출한다. `WorldMapEditor`는 100ms 타이머로 공급자를 읽어 캔버스의 현재 캐릭터와 창 테두리만 갱신하며 설정 저장은 사용자 편집 이벤트에서만 수행한다.

`MinimapCanvas`와 `RouteCanvas`의 기존 로컬 좌표 계산은 삭제하지 않는다. 전역 모드에서만 큰 지도 편집기를 사용하며 기존 테스트 기대값은 유지한다.

- [ ] **Step 4: 통합 UI와 기존 캔버스 테스트 통과 확인**

Run: `py -3.14 -m pytest tests/test_world_map_integration.py tests/test_route_canvas.py tests/test_minimap_canvas.py -v`

Expected: 신규 통합 테스트와 기존 캔버스 테스트 전체 통과.

- [ ] **Step 5: 진입점 통합 커밋**

```bash
git add core_ui/pages.py core_ui/minimap_canvas.py run_integrated.py tests/test_world_map_integration.py tests/test_route_canvas.py
git commit -m "전역 지도 편집기와 실행 진입점 연결"
```

---

### Task 8: 전체 회귀와 실제 게임 인수 검증

**Files:**
- Modify: `03_output/2026-07-14_큰지도_전역좌표_맵핑_checklist_v1.md`
- Modify: `03_output/2026-07-14_큰지도_전역좌표_맵핑_context-notes_v1.md`
- Create: `03_output/2026-07-14_큰지도_전역좌표_맵핑_검증결과_v1.md`

**Interfaces:**
- Consumes: Task 1부터 Task 7까지의 전체 기능.
- Produces: 자동 테스트 결과, 실제 게임 측정값, 남은 위험이 기록된 검증 결과.

- [ ] **Step 1: 핵심 자동 테스트 묶음 실행**

Run:

```bash
py -3.14 -m pytest tests/test_world_map.py tests/test_viewport_tracker.py tests/test_world_graph.py tests/test_world_runner.py tests/test_world_migration.py tests/test_world_position_scanner.py tests/test_world_map_canvas.py tests/test_world_map_editor.py tests/test_world_map_integration.py -v
```

Expected: 전역 지도 관련 테스트 전체 통과.

- [ ] **Step 2: 기존 이동·사냥 회귀 테스트 실행**

Run:

```bash
py -3.14 -m pytest tests/test_block_runner.py tests/test_route_canvas.py tests/test_minimap_canvas.py tests/test_runtime.py tests/test_route_attack.py tests/test_floor_hunt_runner.py tests/test_map_graph.py tests/test_config_adapter.py -v
```

Expected: 기존 관련 테스트 전체 통과.

- [ ] **Step 3: 전체 테스트 실행**

Run: `py -3.14 -m pytest -q`

Expected: 실패 없이 종료 코드 0.

- [ ] **Step 4: 실제 게임 수동 검증**

다음 순서로 한 번의 검증 세션을 기록한다.

1. 큰 지도 이미지를 불러온다.
2. 멀리 떨어진 기준점 두 곳을 큰 지도와 현재 미니맵에서 각각 선택한다.
3. 재투영 오차가 3 로컬 픽셀 이하인지 확인한다.
4. 기존 로컬 설정 변환 전에 백업 파일이 생성되는지 확인한다.
5. 기존 사냥 구역이 같은 실제 위치에 표시되는지 확인한다.
6. 사냥 구역 밖 이동점 두 개와 포탈 액션점 하나를 배치한다.
7. 반복 루트로 구역 밖 이동과 액션이 한 번 이상 성공하는지 확인한다.
8. 같은 노드를 일회성 목적지로 선택해 도달하는지 확인한다.
9. 미니맵 배경을 일부 가려 `estimated` 상태를 유도하고 동작이 계속되는지 확인한다.
10. 가림을 제거한 뒤 `confirmed`로 복구되고 좌표가 실제 위치로 보정되는지 확인한다.

- [ ] **Step 5: 검증 결과와 맥락 기록**

`검증결과_v1.md`에 아래 형식으로 실제 값을 기록한다.

```markdown
# 큰 지도 전역좌표 맵핑 검증 결과

- 자동 테스트: 통과 수 / 전체 수.
- 두 점 재투영 최대 오차: 로컬 픽셀 값.
- 5분 이동 후 전역 좌표 최대 오차: 큰 지도 픽셀 값.
- 구역 밖 이동점 도달: 성공 또는 실패와 원인.
- 액션점 실행: 실제 입력 횟수와 대기 시간.
- 추정 상태 지속 시간: 초.
- 추적 복구 오차: 큰 지도 픽셀 값.
- 기존 로컬 모드 회귀: 성공 또는 실패와 원인.
- 남은 위험: 관찰된 현상과 재현 조건.
```

체크리스트의 완료 항목을 갱신하고 맥락 메모에 실제 게임에서 확인된 좌표 오차와 추적 실패 특성을 추가한다.

- [ ] **Step 6: 최종 검증 커밋**

```bash
git add 03_output/2026-07-14_큰지도_전역좌표_맵핑_checklist_v1.md 03_output/2026-07-14_큰지도_전역좌표_맵핑_context-notes_v1.md 03_output/2026-07-14_큰지도_전역좌표_맵핑_검증결과_v1.md
git commit -m "전역 지도 맵핑 검증 결과 기록"
```

---

## 최종 완료 조건

- 모든 신규·기존 관련 자동 테스트가 통과한다.
- 큰 지도 보정 후 미니맵 창이 움직여도 전역 위치가 유지된다.
- 기존 로컬 설정이 백업 후 정확히 한 번만 전역 좌표로 변환된다.
- 구역 밖 지정 이동점과 제한형 액션점이 실행된다.
- 반복 루트와 일회성 목적지 이동이 같은 연결망에서 동작한다.
- 추적 실패 중에도 시간 제한 없이 동작하고 `estimated` 상태가 표시된다.
- 전역 모드를 끄면 기존 로컬 모드가 동일하게 동작한다.
- 실제 게임 검증 결과와 남은 위험이 `03_output` 문서에 기록된다.
