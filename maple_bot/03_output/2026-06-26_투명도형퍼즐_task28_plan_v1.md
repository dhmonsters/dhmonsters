# Task 28 계획

## 목표
- `puzzle.py`를 실행해서 투명도형 테스트를 바로 시작할 수 있는 진입점을 만든다.
- CLI에서는 `--transparent-test`로 기본 `_record_debug/000_0621_180636_png` replay를 실행한다.
- UI에서는 기본 테스트 버튼으로 같은 replay를 실행한다.

## 범위
- `puzzle.py`에 `--transparent-test`, `--max-frames` 옵션을 추가한다.
- 기본 테스트 replay 경로 helper를 추가한다.
- `PuzzleConsoleWindow`에 기본 테스트 버튼과 실행 메서드를 추가한다.
- 기존 headless replay는 그대로 유지한다.

## 제외
- 라이브 화면 감시와 마우스 입력 자동화는 이번 범위에 넣지 않는다.
