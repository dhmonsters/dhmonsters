# 누락 캐릭터 템플릿 경고 제거 판단 기록

## 2026-08-14 원인 조사

- 프로젝트와 AppData 양쪽 모두 `templates/player/y_p.png`가 현재 존재하지 않는다.
- 템플릿 로더는 파일 존재 여부를 확인하지 않고 OpenCV 읽기를 호출한다.
- 로더는 `y_p.png`와 구형 `r_p.png`를 모두 확인한다.
- 시작 시 생성자 로드와 설정 갱신 로드가 연속 실행되어 파일당 경고가 두 번씩 나타난다.
- 템플릿 목록이 비어 있으면 CharScanner는 기존 색상 감지를 사용하므로 경고 자체는 치명적 오류가 아니다.
- 중복 초기 로드 구조는 이번 범위에서 변경하지 않고, 파일 존재 확인으로 부작용만 제거한다.

## 구현 및 검증 결과

- 템플릿 로더는 `y_p.png` 한 개만 검색한다.
- 후보 경로가 실제 파일일 때만 OpenCV 읽기를 호출한다.
- 사용자 `y_p.png` 우선순위와 캡처 후 런타임 재로딩은 유지한다.
- 관련 템플릿·스캐너 테스트는 `19 passed`, Python 구문 검사는 성공했다.
- 실제 템플릿이 없는 프로젝트 폴더에서 로더를 직접 실행한 결과 경고 없이 `[]`를 반환했다.
- 확장 테스트의 `test_runtime.py` 6개 실패는 제거된 Humanizer, 현재 Pickup 구조, 변경된 연속기 생성자에 대한 기존 기대값이며 이번 수정과 무관하다.
- EXE 빌드, 설치본 생성, 배포는 수행하지 않았다.
- 제품 로더와 회귀 테스트 두 파일은 기존 인덱스와 분리해 커밋 `573ffb5`에만 포함했다.
