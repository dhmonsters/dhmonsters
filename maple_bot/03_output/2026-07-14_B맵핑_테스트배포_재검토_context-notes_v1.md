# B 맵핑 테스트 배포 재검토 판단 기록

## 2026-07-14

- 작업 폴더에 기능과 무관한 사용자 변경 및 대용량 실험 파일이 다수 존재한다.
- 사용자 파일을 섞지 않기 위해 압축본은 현재 `HEAD`의 `maple_bot` 추적 파일만 사용해 생성했다.
- 압축본 이름은 `2026-07-14_maple_bot_B맵핑_테스트배포_v1.zip`으로 확정했다.
- 압축본은 후속 검토나 보완 전에 현재 구현 상태를 보존하는 스냅샷이다.
- 후속 커밋에서는 이번 작업의 압축본과 기록 문서만 선택하며 기존 변경은 건드리지 않는다.
- 압축본 크기는 10,228,358바이트이며 SHA-256은 `35CC49542A63A04FEC064F43A1371EFE936EB02D6BE0BA702BE801614624E9AD`이다.
- 1차 관련 회귀 테스트는 87개가 통과했고 기존 `mss.mss()` 폐기 예정 경고 2개가 발생했다.
- 코드 검토에서 큰 지도 이미지 선택, 이동점 생성, 액션점 생성, 노드 연결 기능이 내부 구조와 실제 화면 사이에 연결되지 않은 중요 누락을 확인했다.
- 위 네 가지 UI 연결과 회귀 테스트 3개를 추가했다.
- 보완 후 관련 회귀 테스트는 90개가 통과했고 동일한 기존 경고 2개만 남았다.
- 실제 게임 및 다른 PC에서의 좌표 보정·이동 검증은 사용자 인수 테스트 단계로 남긴다.
