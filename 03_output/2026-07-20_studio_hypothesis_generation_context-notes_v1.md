# Studio 가설 생성 병목 컨텍스트 노트

## 2026-07-20 시작 상태

- 독립 강체 심판은 커밋 `3c438ff`에서 완료했다.
- 고정 seed 10개 통과율은 67.27%에서 74.00%로 상승했다.
- 실패 390프레임은 selector 133, 가설 생성 147, 박스 중심 복원 43, 후보 부재 67로 분해된다.
- 가장 큰 다음 병목은 후보가 검출됐지만 wide beam 보관 목록에 남지 못한 가설 생성 오류다.
- 현재 wide beam은 폭 16, 상태당 분기 5, 같은 후보당 최대 2상태다.
- 따라서 16개 상태가 16개의 서로 다른 후보를 뜻하지 않는다. 중복 점유가 실제 후보 다양성을 줄이는지 먼저 측정한다.
- GT는 사후 분석에만 사용하며 런타임 규칙에는 넣지 않는다.

## 2026-07-20 원인 분석

- 가설 생성 오류 147프레임 중 145프레임에서 보관함 16칸이 모두 찼다.
- 같은 오류 145프레임에서 동일 후보가 여러 상태를 차지했다.
- 따라서 후보가 검출되지 않은 것이 아니라, 중복 상태가 보관함을 차지해 서로 다른 후보가 밀려난 것이 주원인이었다.
- `width=24`, `branch=5`, 후보 다양성 우선 보관은 후보 중심 oracle 1,384프레임 중 1,373프레임을 보존했다.
- 가설 생성 오류는 147프레임에서 10프레임으로 감소했다.

## 2026-07-20 선택 규칙 검증

- 넓어진 가설을 바로 최종 심판에 넣으면 1,135/1,500이지만 기존 성공 39프레임을 잃어 기각했다.
- 상위 12개만 심판에 넣으면 1,148/1,500이지만 기존 성공 17프레임을 잃어 기각했다.
- 3프레임 연속 확인을 추가하면 기존 성공 손실이 1프레임까지 줄었다.
- 기존 `kinematic_local_rigid` 선택을 보호하자 고정 seed 10개 재생에서 1,109에서 1,123으로 14프레임 개선됐고 기존 성공 손실은 0이었다.
- 공식 기준선 1,110과 재생 기준선 1,109의 1프레임 차이는 기존 trace와 score 정렬 방식 차이다. 동일 재생기 내부 A/B 수치는 1,109 대 1,123으로 비교한다.

## 2026-07-20 라이브 연결

- 기존 폭 16 경로는 현직 경로로 그대로 유지했다.
- 폭 24와 후보 다양성 우선 경로를 도전자 경로로 분리했다.
- 도전자는 상위 12개 가설만 심판에 제출한다.
- 도전자가 3프레임 연속 이어지고 프레임당 이동이 60px 이하여야 현직을 교체한다.
- 현직이 `kinematic_local_rigid`이면 도전자가 교체하지 못한다.
- 동일 고정 seed `codex-rigid-v1-01` 실측은 93/150에서 97/150으로 개선됐다. shadow 예상은 98/150이었다.
- 별도 새 seed `codex-v1` 묶음은 1,392프레임까지 1,021프레임을 통과해 73.35%였다. 1,393번째 GT를 Studio가 보내지 않아 전체 1,500프레임 실행은 시간 초과됐다.
- 새 seed 묶음은 기존 고정 seed와 다른 판이므로 74.00% 기준선의 회귀 비교에는 사용하지 않는다.
- 16GT는 16/16, 평균 오차 26.317640328557086px, coverage 100%를 유지했다.
- selector, identity, evidence, Studio, 녹화 관련 회귀 테스트 150개와 하위 테스트 3개를 통과했다.

## 다음 병목

- 가설 보관 병목은 크게 줄었고, 다음 우선순위는 최종 selector 판별과 박스 내부 중심 복원이다.
- 도전자 경로의 1프레임 shadow/실측 차이는 detector의 실제 후보 변동을 포함한 라이브 반복 실험으로 확인한다.
- Studio 장시간 batch가 마지막 run 중간에서 멈추는 문제는 솔버 정확도와 분리해 하네스 신뢰성 문제로 추적한다.
